spring:
  application:
    name: javalife365api

  # datasource
  datasource:
    url: jdbc:mysql://localhost:3306/javalife365_db?createDatabaseIfNotExist=true
    username: root
    password: root

  # hibernate properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  #mail settings
  mail:
    host: smtp.gmail.com
    port: 587
    username: java.life.365@gmail.com
    password: iqtcpgztonmyaist
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true